<Window x:Class="Lab_rab4_ImamovaAR_BPI_23_02.View.WindowEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Список сотрудников" Height="450" Width="800">

    <DockPanel>
        <StackPanel DockPanel.Dock="Top" 
                    Orientation="Horizontal" 
                    Background="#F3F3F3">

            <Button Content="Добавить" Command="{Binding AddCommand}" Width="90" Height="30" Margin="5"/>
            <Button Content="Изменить" Command="{Binding EditCommand}" Width="90" Height="30" Margin="5"/>
            <Button Content="Удалить" Command="{Binding DeleteCommand}" Width="90" Height="30" Margin="5"/>
        </StackPanel>


        <Grid>
            <ListView ItemsSource="{Binding ListPersonDpo}" SelectedItem="{Binding SelectedPerson}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="40"/>
                        <GridViewColumn Header="Фамилия" DisplayMemberBinding="{Binding LastName}" Width="120"/>
                        <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding FirstName}" Width="120"/>
                        <GridViewColumn Header="Должность" DisplayMemberBinding="{Binding Role}" Width="150"/>
                        <GridViewColumn Header="Дата рождения" 
                                        DisplayMemberBinding="{Binding Birthday, StringFormat={}{0:dd.MM.yyyy}}" 
                                        Width="120"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
</Window>