<Window x:Class="Lab_rab5_ImamovaAR_BPI_23_02.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление сотрудниками" Height="300" Width="650"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Сотрудники" 
                        Command="{Binding OpenEmployeeCommand}" 
                        Width="120" Height="40" Margin="0,0,10,0"/>

                <Button Content="Должности" 
                        Command="{Binding OpenRoleCommand}" 
                        Width="120" Height="40"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Светлая тема" 
                        Command="{Binding ChangeThemeCommand}" 
                        CommandParameter="LightTheme.xaml" 
                        Width="110" Height="30" Margin="5,0"/>

                <Button Content="Темная тема" 
                        Command="{Binding ChangeThemeCommand}" 
                        CommandParameter="DarkTheme.xaml" 
                        Width="110" Height="30" Margin="5,0"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1"/>
    </Grid>
</Window>