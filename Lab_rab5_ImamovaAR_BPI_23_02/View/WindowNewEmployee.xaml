<Window x:Class="Lab_rab5_ImamovaAR_BPI_23_02.View.WindowNewEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Данные сотрудника" Height="450" Width="350" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource WindowTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColorBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#80808080"/>
                </Trigger>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="ToolTip" 
                            Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BirthdayTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="Visibility" Value="Hidden"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <StackPanel Margin="20">
        <TextBlock Text="Код:" Foreground="{DynamicResource WindowTextBrush}"/>
        <TextBox Text="{Binding NewId}" IsReadOnly="True" Opacity="0.7"/>

        <TextBlock Text="Должность:" Foreground="{DynamicResource WindowTextBrush}"/>
        <ComboBox ItemsSource="{Binding ListRole}" 
                  SelectedItem="{Binding NewRole}" 
                  DisplayMemberPath="NameRole"
                  Margin="0,5,0,10" Height="25"/>

        <TextBlock Text="Фамилия:" Foreground="{DynamicResource WindowTextBrush}"/>
        <TextBox Text="{Binding NewLastName, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Имя:" Foreground="{DynamicResource WindowTextBrush}"/>
        <TextBox Text="{Binding NewFirstName, UpdateSourceTrigger=PropertyChanged}"/>
        
        <TextBlock Text="Дата рождения:" Foreground="{DynamicResource WindowTextBrush}"/>
        <Grid>
            <TextBox x:Name="tbBirthday" 
                     Style="{StaticResource BirthdayTextBoxStyle}"
                     Text="{Binding NewBirthday, StringFormat=\{0:dd.MM.yyyy\}, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <DatePicker x:Name="ClBirthday" 
                        SelectedDate="{Binding NewBirthday, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" 
                        Margin="0,5,0,10">
                <DatePicker.Style>
                    <Style TargetType="DatePicker">
                        <Setter Property="Visibility" Value="Hidden"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=tbBirthday, Path=Visibility}" Value="Hidden">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DatePicker.Style>
            </DatePicker>
        </Grid>

        <UniformGrid Columns="2" Margin="0,10,0,0">
            <Button Content="Сохранить" 
                    IsDefault="True"
                    Command="{Binding AcceptCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Margin="5" Height="30"/>
            <Button Content="Отмена" IsCancel="True" Margin="5" Height="30"/>
        </UniformGrid>
    </StackPanel>
</Window>